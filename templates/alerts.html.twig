<div class="container">
    <div class="row justify-content-center">
        <div class="col-md-10">
            {% for flash_message in app.session.flashbag.get('notice') %}
               <div class="alert alert-success mt-2 pb-2" role="alert">
                    {{ flash_message }}
                </div>
            {% endfor %}
        </div>
    </div>
</div>