{% extends 'base.html.twig' %}

{% block title %}Mostrar Categoria{% endblock %}

{% block body %}
    <div class="container">
        <div class="row justify-content-center">
            <div class="col-md-10">
                <div class="card">
                    <div class="card-header">

                        Mostrar Categoria

                        <div class="float-end">
                            <a href="{{ path('category') }}" class="btn btn-sm squred btn-primary">Regresar</a>
                        </div>
                    </div>

                    <div class="card-body">
                         <ul>
                            <li>Name: {{ category.name }}</li>
                        </ul>
                    </div>
                </div>
            </div>
        </div>
    </div>
{% endblock %}
